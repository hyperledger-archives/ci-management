---
# Global Jenkins Defaults
- defaults:
    name: global

    build-days-to-keep: 30
    build-num-to-keep: 40
    build-artifact-days-to-keep: -1
    build-artifact-num-to-keep: 5

    ssh-credentials: 'b706b536-ceb2-4f3b-a616-c483bd16c216'

    global-settings-file: 'global-settings'

    # packer_version
    packer-version: 1.2.3

    # lf-infra defaults
    jenkins-ssh-credential: 'b706b536-ceb2-4f3b-a616-c483bd16c216'
    gerrit-server-name: hyperledger
    lftools-version: <1.0.0

    # Maven defaults
    mvn-version: 'mvn35'
    mvn-goals: '--version'
    mvn-opts: ''
